# Copy to .env and adjust if needed. docker-compose.yml reads these.
# For the default localhost demo the values below already work.

# Shared Flask session secret (any random string).
SECRET_KEY=change-me-please

# Keycloak confidential client secret (must match the realm JSONs in keycloak/).
OIDC_CLIENT_SECRET=nq-cockpit-demo-secret
